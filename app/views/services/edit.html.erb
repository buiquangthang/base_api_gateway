<div>
  <h1 class="font-bold text-2xl mb-3">Editing Service Information</h1>

  <turbo-frame id="<%= dom_id @service %>">
    <%= render "form", service: @service %>
    <%= link_to "Never Mind", services_path, class: "btn mb-3 bg-gray-100" %>
  </div>
</div>
